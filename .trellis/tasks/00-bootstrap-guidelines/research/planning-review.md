# 0 号任务启动前检查记录

检查日期：2026-09-13。结论：启动前规划产物齐备，没有阻塞规划的范围或职责决策；按开发者“直到可以执行 task 之前”的指令，停在 Phase 1.4 激活步骤之前。

这份记录验证规划准备情况，不代表规范已写完、历史已核对或任务已经实施。Phase 1.5 中的用户进入执行确认和 `task.py start` 尚未执行，因此不将整个 Plan → Execute 阶段转换记为完成。

## 产物清单

| 产物 | 状态与职责 |
| --- | --- |
| [prd.md](../prd.md) | 已整理目标、范围、R1–R6 与验收，保留开发者确认约定并补齐四类基础规范覆盖 |
| [design.md](../design.md) | 已明确 11 个 package、首批分组与主题、权威归属、历史核对和上下文设计 |
| [implement.md](../implement.md) | 已给出有序实施清单、验证命令、范围保护和逐文件恢复边界；实施项均未勾选 |
| [planning-context.md](planning-context.md) | 已记录确认规则、只读来源入口与调查限制，不宣称源码调查已完成 |
| [implement.jsonl](../implement.jsonl) | 2 条真实的研究／写作指导条目，加载原因明确 |
| [check.jsonl](../check.jsonl) | 2 条真实的研究／写作指导条目，以审查职责说明用途 |
| [task.json](../task.json) | 已同步中文标题、描述、`package: plateau` 和无重复的关联范围；身份、初始化状态和无关元数据保留 |

两份清单当前引用同一组稳定资料，原因分别面向执行和检查。PRD、设计、实施文档由本地角色上下文读取器自动组合，未在 JSONL 中重复列出。新 spec 尚未建设，不将目标路径、待修改模板或业务源码作为预注入正文。

## 实际检查结果

| 检查 | 结果 |
| --- | --- |
| `get_context.py` 与 Phase 1.1／1.3／1.4／1.5 指引 | 已读取当前工作流；当前会话无绑定任务，00 号任务仍为初始化的 `in_progress` |
| `get_context.py --mode packages --json` | 11 个 package 与预期一致，默认 `plateau`；所有源码路径存在；现有 spec 仍是待整理的初始化分组 |
| `task.py validate .trellis/tasks/00-bootstrap-guidelines` | 通过；implement／check 各 2 条有效记录，无占位行 |
| 现有 `get_implement_context`／`get_check_context` 的只读预览 | 两种角色各完整加载清单的 2 份正文和 3 份规划，共 5 份文档；逐份全文核对，无遗漏、截断或读取警告 |
| 任务元数据检查 | ID、name、status、scope、meta、Git 相关状态及其他无关字段保留；relatedFiles 无重复并覆盖全部 package |
| 调查入口检查 | planning-context 中列明的来源路径真实存在；仅核对路径，不据此推断 API 行为 |
| 保护范围检查 | 169 个既有文件的摘要一致，覆盖 spec、配置、workflow、托管脚本、AGENTS、skills、Codex 文件和 Git index |
| 文档格式与引用 | 检查本任务规划 Markdown 的尾随空白、末尾换行、代码围栏和本地文档链接，包含未跟踪文件 |
| `git diff --check` | 通过；与未跟踪规划文档的单独检查配合使用 |
| `task.py current` | 无输出、退出码 1；与会话上下文的“无当前任务”一致，没有执行激活 |

只读预览直接调用现有读取函数，未发送伪造 hook 事件、未设置会话标识、未启动子代理，也未修改托管脚本。该结果证明资料可被本地读取器完整组合；宿主实际注入在执行会话中另行检查。

## 需求与设计收敛审阅

- R1／R2：共享与框架职责、业务端命名、默认项和来源边界明确；占位应用不虚构实现。
- R3：保留按主题组织与有效索引，兼容现有发现机制；不强制模板文件清单。
- R4：AGENTS 指定章节、server 边界、biz/data 顺序、专有 internal 模块、CRUD 流程，以及测试／API／生命周期／业务前端基础覆盖均已进入规划。
- R5：先建立当前基线再核对历史；用户确认规范与当前实现分开表达，缺陷和未验收能力如实记录。
- R6：三份中文规划与两份真实清单齐备，元数据与加载职责一致；验收项仍待实施后依据证据勾选。
- PRD 已按最终结构复核，目标、范围、需求和验收各自承担职责，无未处理的临时讨论章节或阻塞性开放问题。

## 留到执行阶段的工作

源码细读与 CRUD 调用契约核对、规范正文建设、历史覆盖表及实现差异表、旧模板归并、导航更新和最终验收都按 implement.md 执行。框架与业务实现需要整改时只记录问题，另行处理。

本轮没有运行 `task.py start`、生成 spec 正文、修改 AGENTS／业务代码／工作区联调，也没有提交、推送或归档任务。下次明确进入执行后，从 implement.md 第 1 节开始。
