// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// AuditEventItem mirrors the ClickHouse audit_events row.
export type AuditEventItem = {
  eventId: string | undefined;
  eventType: string | undefined;
  eventVersion: string | undefined;
  occurredAt: wellKnownTimestamp | undefined;
  service: string | undefined;
  operation: string | undefined;
  actorId: string | undefined;
  actorType: string | undefined;
  actorDisplayName: string | undefined;
  targetType: string | undefined;
  targetId: string | undefined;
  targetName: string | undefined;
  success: boolean | undefined;
  errorCode: string | undefined;
  errorMessage: string | undefined;
  traceId: string | undefined;
  requestId: string | undefined;
  // detail is a JSON string serialized from the proto oneof detail field.
  detail: string | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type ListAuditEventsRequest = {
  startTime: wellKnownTimestamp | undefined;
  endTime: wellKnownTimestamp | undefined;
  eventTypes: string[] | undefined;
  actorId: string | undefined;
  service: string | undefined;
  pageSize: number | undefined;
  pageToken: string | undefined;
};

export type ListAuditEventsResponse = {
  events: AuditEventItem[] | undefined;
  nextPageToken: string | undefined;
};

export type CountAuditEventsRequest = {
  startTime: wellKnownTimestamp | undefined;
  endTime: wellKnownTimestamp | undefined;
  eventTypes: string[] | undefined;
  actorId: string | undefined;
  service: string | undefined;
};

export type CountAuditEventsResponse = {
  totalCount: number | undefined;
};

// AuditQueryService provides read-only access to persisted audit events.
export interface AuditQueryService {
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createAuditQueryServiceClient(
  handler: RequestHandler
): AuditQueryService {
  return {
  };
}
export interface AuditHTTPService {
  ListAuditEvents(request: ListAuditEventsRequest): Promise<ListAuditEventsResponse>;
  CountAuditEvents(request: CountAuditEventsRequest): Promise<CountAuditEventsResponse>;
}

export function createAuditHTTPServiceClient(
  handler: RequestHandler
): AuditHTTPService {
  return {
    ListAuditEvents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/audit/events`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.startTime) {
        queryParams.push(`startTime=${encodeURIComponent(request.startTime.toString())}`)
      }
      if (request.endTime) {
        queryParams.push(`endTime=${encodeURIComponent(request.endTime.toString())}`)
      }
      if (request.eventTypes) {
        request.eventTypes.forEach((x) => {
          queryParams.push(`eventTypes=${encodeURIComponent(x.toString())}`)
        })
      }
      if (request.actorId) {
        queryParams.push(`actorId=${encodeURIComponent(request.actorId.toString())}`)
      }
      if (request.service) {
        queryParams.push(`service=${encodeURIComponent(request.service.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.pageToken) {
        queryParams.push(`pageToken=${encodeURIComponent(request.pageToken.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AuditHTTPService",
        method: "ListAuditEvents",
      }) as Promise<ListAuditEventsResponse>;
    },
    CountAuditEvents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/audit/events:count`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.startTime) {
        queryParams.push(`startTime=${encodeURIComponent(request.startTime.toString())}`)
      }
      if (request.endTime) {
        queryParams.push(`endTime=${encodeURIComponent(request.endTime.toString())}`)
      }
      if (request.eventTypes) {
        request.eventTypes.forEach((x) => {
          queryParams.push(`eventTypes=${encodeURIComponent(x.toString())}`)
        })
      }
      if (request.actorId) {
        queryParams.push(`actorId=${encodeURIComponent(request.actorId.toString())}`)
      }
      if (request.service) {
        queryParams.push(`service=${encodeURIComponent(request.service.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AuditHTTPService",
        method: "CountAuditEvents",
      }) as Promise<CountAuditEventsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
