// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable no-use-before-define, @typescript-eslint/no-unused-vars, prettier/prettier */

interface TransportMeta {
  service: string;
  method: string;
}

export interface ClientTransport {
  unary<T>(
    path: string,
    method: string,
    body: null | string,
    meta: TransportMeta,
  ): Promise<T>;
  serverStream<T>(path: string, meta: TransportMeta): ServerStream<T>;
  duplexStream<TIn, TOut>(path: string, meta: TransportMeta): DuplexStream<TIn, TOut>;
}

export interface ServerStream<T> {
  onEvent(listener: (data: T) => void): () => void;
  onError(handler: (error: Error) => void): void;
  close(): void;
}

export interface DuplexStream<TIn, TOut> extends ServerStream<TOut> {
  send(data: TIn): void;
}

function encodePathSegment(value: unknown): string {
  return encodeURIComponent(String(value)).replace(/[!'()*]/g, (character) => `%${character.charCodeAt(0).toString(16).toUpperCase()}`);
}

function encodeMultiSegmentPath(value: unknown): string {
  return String(value)
    .split('/')
    .map(encodePathSegment)
    .join('/');
}

// AuditEventItem mirrors the ClickHouse audit_events row.
export type AuditEventItem = {
  actorDisplayName?: string;
  actorId?: string;
  actorType?: string;
  // detail is a JSON string serialized from the proto oneof detail field.
  detail?: string;
  errorCode?: string;
  errorMessage?: string;
  eventId?: string;
  eventType?: string;
  eventVersion?: string;
  occurredAt?: wellKnownTimestamp;
  operation?: string;
  requestId?: string;
  service?: string;
  success?: boolean;
  targetId?: string;
  targetName?: string;
  targetType?: string;
  traceId?: string;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type ListAuditEventsRequest = {
  actorId?: string;
  endTime?: wellKnownTimestamp;
  eventTypes?: string[];
  pageSize?: number;
  pageToken?: string;
  service?: string;
  startTime?: wellKnownTimestamp;
};

export type ListAuditEventsResponse = {
  events?: AuditEventItem[];
  nextPageToken?: string;
};

export type CountAuditEventsRequest = {
  actorId?: string;
  endTime?: wellKnownTimestamp;
  eventTypes?: string[];
  service?: string;
  startTime?: wellKnownTimestamp;
};

export type CountAuditEventsResponse = {
  totalCount?: string;
};

// AuditQueryService provides read-only gRPC and HTTP access to persisted audit events.
export interface AuditQueryService {
  ListAuditEvents(
    request: ListAuditEventsRequest,
  ): Promise<ListAuditEventsResponse>;
  CountAuditEvents(
    request: CountAuditEventsRequest,
  ): Promise<CountAuditEventsResponse>;
}

export function createAuditQueryServiceClient(
  transport: ClientTransport,
): AuditQueryService {
  return {
    ListAuditEvents(request) {
      const path = `v1/audit/events`;
      const body = null;
      const queryParams: string[] = [];
      if (request.startTime !== undefined && request.startTime !== null) {
        queryParams.push(
          `startTime=${encodeURIComponent(request.startTime.toString())}`,
        );
      }
      if (request.endTime !== undefined && request.endTime !== null) {
        queryParams.push(
          `endTime=${encodeURIComponent(request.endTime.toString())}`,
        );
      }
      if (request.eventTypes !== undefined && request.eventTypes !== null) {
        request.eventTypes.forEach((x) => {
          queryParams.push(
            `eventTypes=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.actorId !== undefined && request.actorId !== null) {
        queryParams.push(
          `actorId=${encodeURIComponent(request.actorId.toString())}`,
        );
      }
      if (request.service !== undefined && request.service !== null) {
        queryParams.push(
          `service=${encodeURIComponent(request.service.toString())}`,
        );
      }
      if (request.pageSize !== undefined && request.pageSize !== null) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.pageToken !== undefined && request.pageToken !== null) {
        queryParams.push(
          `pageToken=${encodeURIComponent(request.pageToken.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<ListAuditEventsResponse>(uri, 'GET', body, {
        service: 'AuditQueryService',
        method: 'ListAuditEvents',
      });
    },
    CountAuditEvents(request) {
      const path = `v1/audit/events:count`;
      const body = null;
      const queryParams: string[] = [];
      if (request.startTime !== undefined && request.startTime !== null) {
        queryParams.push(
          `startTime=${encodeURIComponent(request.startTime.toString())}`,
        );
      }
      if (request.endTime !== undefined && request.endTime !== null) {
        queryParams.push(
          `endTime=${encodeURIComponent(request.endTime.toString())}`,
        );
      }
      if (request.eventTypes !== undefined && request.eventTypes !== null) {
        request.eventTypes.forEach((x) => {
          queryParams.push(
            `eventTypes=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.actorId !== undefined && request.actorId !== null) {
        queryParams.push(
          `actorId=${encodeURIComponent(request.actorId.toString())}`,
        );
      }
      if (request.service !== undefined && request.service !== null) {
        queryParams.push(
          `service=${encodeURIComponent(request.service.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<CountAuditEventsResponse>(uri, 'GET', body, {
        service: 'AuditQueryService',
        method: 'CountAuditEvents',
      });
    },
  };
}
export class ApiClient {
  private _auditQueryService?: AuditQueryService;
  private readonly _transport: ClientTransport;

  constructor(transport: ClientTransport) {
    this._transport = transport;
  }

  get auditQueryService(): AuditQueryService {
    return this._auditQueryService ??= createAuditQueryServiceClient(this._transport);
  }
}

export function createApiClient(transport: ClientTransport): ApiClient {
  return new ApiClient(transport);
}


// @@protoc_insertion_point(typescript-http-eof)
