# AGENTS.md - app/example/service/cmd/server/

<!-- Parent: ../../AGENTS.md -->
<!-- Updated: 2026-07-21 -->

## Scope

Wire-generated entrypoint for the runnable User CRUD reference service.

## Rules

- `main.go` owns process startup/shutdown only; application dependencies come from providers.
- `wire_gen.go` is generated by `make wire`/`make gen` and must never be edited manually.
- Verify through `make run`; do not embed migration, test database, or frontend lifecycle logic in the entrypoint.
