// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Servora-Kit/plateau/app/iam/service/internal/data/ent/httpsession"
)

// HTTPSession is the model entity for the HTTPSession schema.
type HTTPSession struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Data holds the value of the "data" field.
	Data []byte `json:"-"`
	// Expiry holds the value of the "expiry" field.
	Expiry       time.Time `json:"expiry,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HTTPSession) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case httpsession.FieldData:
			values[i] = new([]byte)
		case httpsession.FieldID:
			values[i] = new(sql.NullString)
		case httpsession.FieldExpiry:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HTTPSession fields.
func (_m *HTTPSession) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case httpsession.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case httpsession.FieldData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field data", values[i])
			} else if value != nil {
				_m.Data = *value
			}
		case httpsession.FieldExpiry:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiry", values[i])
			} else if value.Valid {
				_m.Expiry = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HTTPSession.
// This includes values selected through modifiers, order, etc.
func (_m *HTTPSession) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this HTTPSession.
// Note that you need to call HTTPSession.Unwrap() before calling this method if this HTTPSession
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HTTPSession) Update() *HTTPSessionUpdateOne {
	return NewHTTPSessionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HTTPSession entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HTTPSession) Unwrap() *HTTPSession {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: HTTPSession is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HTTPSession) String() string {
	var builder strings.Builder
	builder.WriteString("HTTPSession(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("data=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("expiry=")
	builder.WriteString(_m.Expiry.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// HTTPSessions is a parsable slice of HTTPSession.
type HTTPSessions []*HTTPSession
